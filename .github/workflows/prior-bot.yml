name: prior.agent

on:
  # Roughly every 4 hours (6 posts/day — well under X Free tier 17/day limit)
  schedule:
    - cron: '17 */4 * * *'
  # Manual trigger from GitHub UI
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no actual X post)'
        required: false
        default: 'false'

# Allow committing the updated api/log.json back to the repo
permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: Install deps
        run: pip install -r scripts/requirements.txt

      - name: Run prior_bot.py
        env:
          ANTHROPIC_API_KEY:     ${{ secrets.ANTHROPIC_API_KEY }}
          X_API_KEY:             ${{ secrets.X_API_KEY }}
          X_API_SECRET:          ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN:        ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          PRIOR_DRY_RUN:         ${{ github.event.inputs.dry_run == 'true' && '1' || '0' }}
        run: python scripts/prior_bot.py

      - name: Commit updated log
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "prior.agent"
          git config user.email "prior-bot@users.noreply.github.com"
          if [[ -n "$(git status --porcelain api/log.json)" ]]; then
            git add api/log.json
            git commit -m "prior: log update $(date -u +'%Y-%m-%dT%H:%MZ')"
            git push
          else
            echo "no changes to log"
          fi
